tasks:
  ExtractAndSaveNews:
    command:
      - python
      - main.py

#condaConfigFile: conda.yaml
artifactsDir: artifacts/
variables:
  search_phrase: ${RC_WORK_ITEM:search_phrase}
  news_category: ${RC_WORK_ITEM:news_category}
  months: ${RC_WORK_ITEM:months}